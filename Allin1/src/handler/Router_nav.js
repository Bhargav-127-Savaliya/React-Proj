import React from "react";

const NavBar = (props) => {
  return (
    <React.Fragment>
        <div>
            <h2>Welcome Router</h2>
        </div>
    </React.Fragment>
  )
};

export default NavBar;
